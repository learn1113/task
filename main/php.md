## str_replace  
```
str_replace(
    mixed $search,      // 要查找的目标（字符/数组）
    mixed $replace,     // 替换值（字符/数组）
    mixed $subject,     // 被搜索的字符串/数组
    int &$count = null  // 可选：替换次数的引用变量
): mixed
```